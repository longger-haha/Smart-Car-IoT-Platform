# 智能小车 (SmartRover) 全栈系统功能与安全需求规划书 (PRD)

> **修订说明**: 本次修订在原有 Web 平台需求之上，补充了“端侧车辆硬件控制系统（Arduino 传感器、算法、执行器）”的精细化设计，并将地理定位、路线规划及核心安全体系进行了深度穿插融合，形成完整的“端-云-安全”全架构规划。

---

## 第一篇：边缘侧车辆端系统结构 (基于 Arduino / ESP 主控)

> **设计基准**：此部分负责实体车辆的环境感知、自动驾驶决策与运动执行。

### 模块 1：多维环境感知与数据采集 (Sensor Module)
*   **1.1 精准避障探测**：搭载**超声波传感器**与**红外线传感器**阵列，实时捕获车辆前方与侧面障碍物的距离反馈。
*   **1.2 视觉与图传采集**：搭载**微型摄像头模块**（如OV2640），将环境画面编码推送至云端。
*   **1.3 绝对位置感知**：集成 **GPS/北斗双模定位传感器**，向云端高频推流当前高精度经纬度坐标。

### 模块 2：车载核心控制算法设计 (Control Algorithms)
*   **2.1 基于 PID 的平顺运动控制**：在 Arduino 微控制器中封装 PID 控制算法，对电机转速进行闭环调节，杜绝小车在加减速过程中的抖动及跑偏现象。
*   **2.2 模糊逻辑决策算法 (Fuzzy Logic)**：针对传感器传回的非结构化障碍物数据（例如：前方 20cm 且 左侧 15cm 皆有障碍物），采用模糊逻辑控制算法取代死板的 `if-else`，使车辆在自动巡航模式下能做出更拟人化、平滑的避障机动动作。

### 模块 3：执行机构动力驱动 (Actuators)
*   **3.1 PWM 无级动力驱动**：主控板输出高精度的 PWM（脉宽调制）信号，通过 L298N/TB6612 等电机驱动模块，实现对直流驱动电机速度的细腻分配。
*   **3.2 精确转向伺服系统**：发送特定的 PWM 周期波控制前端**转向舵机**，配合 PID 算法，完成云端路线规划下达后的高精度角度转向（如设定转角为 25.5 度）。

---

## 第二篇：云侧物联网平台应用系 (基于 Vue 3 + Python Flask + MySQL)

> **设计基准**：此部分为系统的 Web 控制中枢，提供对实车的数据总览、干预控制与监控。

### 模块 4：GIS 地理联动与智能调度系统 
*   **4.1 车辆实时定位映射 (Location Tracking)**：Web 平台嵌入高德/百度地图，基于 Arduino GPS 模块上报的真实经纬度，实时更新车辆在虚拟地图中的图标位置。
*   **4.2 全局路线规划与下发 (Route Planning)**：用户在 Web 页面地图上点击打点生成一条期望行进路线（Waypoints），平台通过路线规划算法生成坐标数组，随后通过通信协议将“巡航任务包”发送至 Arduino 控制器，由其调用 PID 算法自动驶向终点。
*   **4.3 MySQL 历史时空轨迹与数据中心**: 车辆上传的 GPS 流、传感数据（温湿度/超声波）将高频存入 MySQL 关系型数据库。这不仅支撑前端随时调用展示某天的历史行驶轨迹，更为平台的“安全防抵赖审计”提供不可变的数据存根。

### 模块 5：多源传感大盘与图传监控中心
*   **5.1 仪表盘与可视化大屏**：利用 Echarts 以动态折线图的形式，绘制环境温度、近距离超声波探测距离等数据流。
*   **5.2 FPV 视频流呈现**：在控制面板嵌入来自硬件侧摄像头模块投递的高清视频画面。

### 模块 6：远程人为接管控制系统
*   拥有独立的**虚拟操纵摇杆/键盘控制面板**，当车辆端测得的“模糊控制算法”遇到极端死角时，允许平台用户下发权限接管指令，强制用 PWM 操控执行器倒车或转向。

---

## 第三篇：全链路安全防护体系 (物联网安全深度融合体系)

> **设计基准**：车辆网络一旦被入侵，可能导致硬件失控甚至引发实体伤害。此篇章是针对安全课答辩的核心加分项，将安全机制贯穿前两篇的每一个环节。

### 模块 7：车辆防劫持：严格鉴权与控制流安全
*   **7.1 车辆零信任入网 (Device IAM)**：杜绝匿名设备向控制平台发送垃圾传感器数据。车辆主控连接平台时，必须携带平台颁发的动态 `SecretToken` 与数字证书，阻截未知伪造硬件连接。
*   **7.2 执行器指令防重放与防篡改 (Anti-Replay Attack)**：控制车辆“前进/转向/左转”的指令非常敏感。因此，下发给 Arduino 的方向及 PWM 设定参数，**必须附加防重放的时间戳并进行 SHA-256 签名计算**。
    *   *答辩演示点*：如果黑客截获了“向左转”的报文，10 分钟后尝试再次向原端口发送（重放），系统发现时间戳过期或签名失效，安全大屏将立马闪烁红灯：“`拦截非法陈旧越权控制帧，丢弃指令`”。

### 模块 8：传感防欺骗：全链路数据透明加解密
*   **8.1 传感器数据加密输送 (Data Encryption)**：对于车辆定位数据、图传数据，禁用明文交互。在车辆侧使用轻量级加密标准将其加密为密文。
*   **8.2 直观解密查验日志 (Crypto Audit)**：
    *   *答辩演示点*：专门设计一个页面，左侧窗口实时刷新抓取的真实密文（乱码），右半边显示后端通过密钥解析出的定位 GPS 坐标。完美证明了“黑客抓包也拿不到车在哪里”的核心安全诉求。

### 模块 9：系统边界安全防线 (RBAC 与应用层防火墙)
*   **9.1 基于角色的越权阻断 (Privilege Control)**：拥有多种操作用户（管理员、只读访客），当无权限登录者在前端尝试向执行器下达指令时，后端鉴权即刻拦截，弹出抢眼的“越权操作违规”警告窗口。
*   **9.2 异常流量阻断控制**：在平台设置防 DDos 检测，若某个 IP 或设备单秒发包超过正常传感器的阈值，直接锁定该来源。
