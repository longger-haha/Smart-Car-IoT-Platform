# 智能小车 (SmartRover) 平台功能需求规划书 (PRD)

> **设计基线**: 平台必须建立在扎实的“基础物联网业务”之上（如设备映射、定位绘制、遥控操纵等），在主干功能闭环后，再进一步构筑安全防护体系。
> **技术栈**: 前端采用 **Vue3 + Element Plus**，后端采用 **Python Flask**。

---

## 第一篇：基础物联网应用大系 (基础必须拿下的功能)

既然是做小车的物联网系统，“控制”与“地图”将是这个平台最出彩、最具有“乐维”那样工业级效果的核心。

### 模块 1：高水平 GIS 地图联动 (核心应用功能)
*   **1.1 实时卫星定位 (Real-time Positioning)**：
    *   基于前端 Vue 引入 **高德地图 API 或 百度地图 API**。
    *   接收硬件设备（如带有 GPS 模块的 ESP32）上报的经纬度数据，**实时刷新小车在 Web 地图上的行驶坐标**。
*   **1.2 沿途历史轨迹回放 (Historical Trajectory)**：
    *   用户在时间选择器选择日期，系统从 MySQL 拉出过去某一时段的坐标点集合，前端根据点位在地图上实时平滑回放小车曾经的行驶轨迹路线。
*   **1.3 云端路线规划下发 (Route Planning & Waypoints)**：
    *   用户可在网页版高德地图上点击“设定起点、途径点、终点”。
    *   利用高德的路线规划算法算出总路线后，由 Python 后端将**坐标数组集打包**，通过 MQTT 协议下发给小车主控芯片，令其启动“自巡航任务”。

### 模块 2：物联环境感知大盘 (Sensing Dashboard)
*   **2.1 环境温湿度监测大盘**：直观展示小车搭载的温湿度传感器返回数据的 Echarts 波动折线图，可支持预设极值报警（如周围温度超 60 度则亮起火灾黄色预警）。
*   **2.2 雷达数字避障仪表盘**：通过小车上的超声波或红外雷达模块反馈的“距离”，在前端通过仪表盘的形式显示“前方距离障碍物还有 X 厘米”。
*   **2.3 本机运行状态实时监控**：在侧边栏显示小车的剩余电池电量估算、Wi-Fi/移动基站信号强弱等底层“健康数据”。

### 模块 3：远程虚拟驾驶操纵台 (Teleoperation Center)
*   **3.1 虚拟操作摇杆 / 方向键盘控制台**：
    *   在 Web 端实现左转/右转/前进/急停等界面按钮模块（含键盘 W/A/S/D 按键监听绑定）。
    *   产生极低延迟的方向指令帧，越过常规轮询接口，直接走 MQTT Pub 向特定小车下达实时电机转动参数。
*   **3.2 FPV 视频流图传视窗（扩展功能，如果硬件搭载摄像头）**：
    *   接收来自设备端（如 ESP32-CAM 模块）发送的视频流并在操作台中直播呈现。

### 模块 4：设备与档案生命周期管理 (Device Center)
*   **4.1 数字设备大厅**：列表化展示项目中所有的接入智能设备（不仅是主车，也可能是车窗、轮胎传感器）。
*   **4.2 设备状态启停**：展示设备 MAC 地址、目前所处固件版本号。如果设备处于掉线（未按时发送心跳包）状态，将展示为灰色的“离线”。

---

## 第二篇：内生安全防护体系 (用于高水平安全答辩)

在上述地图和遥控都跑起来以后，再向平台内强行注入“安全攻防拦截”理念以应付题目要求。

### 模块 5：平台级防越权与零信任门禁 (Device & User IAM)
*   **5.1 一机一密身份核发**：小车连接物联网不是随便连个 Wi-Fi 就行的，必须要在 Web 端为该硬件核发一对强随机的 `DeviceKey` 和 `SecretToken` 才能握手入网。
*   **5.2 越权操控拦截演示 (RBAC 拦截)**：针对“模块 3”的远程操纵台，分配具有“司机”和“无权访客”两套登录密码。访客一旦企图按前进按钮，即刻在视图顶端弹出抢眼的红色警告框：“`鉴权阻拦系统：您无权限操控此车！`”。

### 模块 6：透明加解密与通信完整性审计 (Crypto Audit)
*   **6.1 直观防嗅探对比面板**：专门开辟一个页面，一分为二：
    *   左半边展示设备发送给云端的“真实密文报文”（加密乱码，证明黑客即便抓包也拿不到诸如 GPS 地理位置这样的数据）。
    *   右半边展示云端 Python 通过 AES 解析出来的真实温度明文、坐标明文。
    *   （用于向导师交差，完美展示了“我并不是在平台里空喊口号，我真做了加密传输”）。

### 模块 7：防重放劫持拦截系统 (Anti-Replay Attack)
*   **7.1 异常丢弃与预警灯**：小车的按键下发/心跳指令附加极短时间戳和动态身份哈希。如果后端检测到收到一秒钟之前的旧指令人为复制攻击，前端立刻产生红色系统弹窗：“`拦截到异常！系统遭到时效过期指令劫持并已成功阻断！`”。
