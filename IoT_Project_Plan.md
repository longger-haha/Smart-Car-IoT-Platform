# SmartRover: 乐维风格物联网安全平台开发规划方案

## 1. 项目全局框架选型

### (1) 服务器购买与系统
*   **推荐方案**：购买云服务器（阿里云/腾讯云），选择 **Linux (Ubuntu Server 22.04 LTS)**。
*   **原因**：物联网所需的 MQTT 中间件、Redis、数据库等在 Linux 和 Docker 下运行效率最高、部署最简单。通过 Docker，几分钟即可完成生产环境搭建。

### (2) 后端架构
*   **推荐栈**：**Python Flask** 
*   **优势**：相较于 Java，Flask 非常轻量，适合两周短周期开发。且 Python 在密码学处理（AES、RSA加密）、安全脚本编写（抓包、渗透测试）方面拥有压倒性优势，非常契合“安全”主题。

### (3) 前端架构 (针对零基础团队)
*   **推荐栈**：**Vue3 + Element Plus (借用现成模板)**
*   **策略**：从 GitHub 下载开源的后台管理系统模板（如 `vue-admin-template` 或 `Geeker-Admin`）。你们无需从头写 CSS 和布局，只需了解前端发请求（Axios）并在图表插件（ECharts）上把获取的数据呈现出来。

---

## 2. 硬件采购清单 (边缘感知与控制侧)

针对“智能小车”或一般物联网安全验证平台，推荐以下采购：

| 模块分类 | 具体型号 | 用途说明 |
| :--- | :--- | :--- |
| **主板核心** | **ESP32 开发板** | 自带 Wi-Fi 与蓝牙。性能足够运行加密算法（如 mbedTLS），是理想的物联网安全端点硬件。 |
| **感知模块** | DHT11(温湿度) / MQ-2(烟雾) | 读取环境数据，向云平台不间断推流。 |
| **执行模块** | 小车电机 + L298N驱动 / 继电器 | 用于在 Web 端验证平台的“指令下发”与安全权限验证控制。 |
| **安全附加件** | RC522 (RFID 模块 - 可选) | 可模拟刷卡/设备绑定认证、权限鉴别等实体安全场景。 |
| **耗材** | 杜邦线（各一扎）、面包板 | 基础连接与实验必需品。 |

*注：前期无需等待硬件到货，直接用 Python 编写“模拟设备脚本”向后端发送虚假感应数据，实现前后端分离开发。*

---

## 3. 三人团队分工与里程碑 (两周冲刺)

### 团队分工建议
*   **A 同学（后端与架构）**：买服务器，搭建 Linux 环境 (部署 Docker)。编写 Flask API 接口（用户注册登录、设备列表、指令下发等），管理数据库。
*   **B 同学（前端与UI）**：跑通前端 Vue 模板。做界面换皮，接入 Echarts 实现传感器历史数据折线图。
*   **C 同学（物联通信与安全）**：负责搭建/配置 Mosquitto (MQTT 服务)。搭建软硬件通信桥梁（编写设备端 Python 模拟器或 ESP32 C 代码），设计数据加密包。

### 时间里程碑
#### 第一周：数据流动与基础系统打通
*   【环境】部署云端 MySQL、配置 MQTT 容器。
*   【后端】发布最基础的 POST/GET 接口（接收数据、获取设备状态）。
*   【模拟】C同学写 Python 脚本充当传感器，不间断通过 MQTT 发数据给服务器。
*   【前端】模板成功运行，能够写死假数据显示一个监控看板页。

#### 第二周：安全加固与平台联调
*   【联调】前端请求后端，后端下发 MQTT 到模拟脚本/硬件，实现真实的前后端全链路打通。
*   【安全】**传输层加密：** 加入安全逻辑（见第四节）；加入前端登录鉴权机制（JWT Token）。
*   【部署】将前端构建产物（dist）和后端容器在服务器上实现公网闭环上线。

---

## 4. 核心：物联网安全体系切入点规划

既然是物联网“安全”答辩，必须着重体现防范攻击的能力。这是你们项目的亮点：

1.  **端到端协议安全 (MQTTS)**：放弃明文的端口 1883。在 Broker 端生成系统证书，通过 MQTTS (TCP 8883) 或 WSS 端口传输，防止 Wireshark 等工具局域网嗅探。
2.  **“零信任”设备接入鉴权**：在平台设置一个“设备注册机制”。ESP32 启动时不能随便给 MQTT 发消息，必须携带系统分配的 `Secret Key` 或 JWT 凭据进行身份校验，防止僵尸设备接入耗尽资源。
3.  **防篡改与重放攻击防御**：在业务逻辑层面，设备上报报文加入**时间戳**与**哈希签名(SHA256)**。服务器若收到旧时间戳或签名不对应的内容，直接丢弃并在 Web 控制台展示红色的“告警栏：检测到重放/伪造攻击”。
4.  **云端行为监测预警**：在后端加一个限流阀，如果某个 IP 或者某个 DeviceID 在 1 秒钟内发包 100 次，在 Web 前端立马弹窗提示“受到 DDoS/泛洪攻击”，并临时封禁该设备。
